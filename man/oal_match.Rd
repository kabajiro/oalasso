% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/handoff.R
\name{oal_match}
\alias{oal_match}
\title{Match on an outcome-adaptive lasso propensity score}
\usage{
oal_match(object, ...)
}
\arguments{
\item{object}{An \code{oal} object.}

\item{...}{Arguments forwarded verbatim to \code{\link[MatchIt:matchit]{MatchIt::matchit()}} (e.g.,
\code{method}, \code{caliper}, \code{ratio}, \code{replace}).}
}
\value{
A \code{matchit} object; see \code{\link[MatchIt:matchit]{MatchIt::matchit()}}.
}
\description{
Convenience pass-through to \code{\link[MatchIt:matchit]{MatchIt::matchit()}}: matches on the propensity
score of an \code{\link[=oal]{oal()}} fit, reusing the formula and data stored in the object.
Equivalent to the canonical explicit call
\preformatted{MatchIt::matchit(<formula>, data = <data>, distance = fit$ps, ...)}
but with no opportunity for row misalignment between the two steps. All
\code{...} arguments are forwarded verbatim; the return value is an ordinary
\code{matchit} object, so the full \pkg{MatchIt}/\pkg{cobalt} toolkit applies.
}
\examples{
\dontshow{if (requireNamespace("MatchIt", quietly = TRUE)) withAutoprint(\{ # examplesIf}
data("lalonde", package = "MatchIt")
fit <- oal(treat ~ age + educ + married + re74, data = lalonde,
           outcome = ~ re78)
m <- oal_match(fit, method = "nearest", caliper = 0.2)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=oal]{oal()}}, \code{\link[=oal_weight]{oal_weight()}}, \code{\link[MatchIt:matchit]{MatchIt::matchit()}}
}
