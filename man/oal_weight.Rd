% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/handoff.R
\name{oal_weight}
\alias{oal_weight}
\title{Weight by an outcome-adaptive lasso propensity score}
\usage{
oal_weight(object, estimand = object$estimand, ...)
}
\arguments{
\item{object}{An \code{oal} object.}

\item{estimand}{The estimand passed to \code{\link[WeightIt:weightit]{WeightIt::weightit()}}; defaults to
the estimand of the fit (note that the wAMD \emph{selection} already used the
fitted estimand's weights).}

\item{...}{Arguments forwarded verbatim to \code{\link[WeightIt:weightit]{WeightIt::weightit()}}.}
}
\value{
A \code{weightit} object; see \code{\link[WeightIt:weightit]{WeightIt::weightit()}}.
}
\description{
Convenience pass-through to \code{\link[WeightIt:weightit]{WeightIt::weightit()}}: constructs balancing
weights from the propensity score of an \code{\link[=oal]{oal()}} fit, reusing the stored
formula and data. Equivalent to the canonical explicit call
\preformatted{WeightIt::weightit(<formula>, data = <data>, ps = fit$ps, estimand = <estimand>, ...)}
All \code{...} arguments are forwarded verbatim; the return value is an ordinary
\code{weightit} object.
}
\examples{
\dontshow{if (requireNamespace("MatchIt", quietly = TRUE) && requireNamespace("WeightIt", quietly = TRUE)) withAutoprint(\{ # examplesIf}
data("lalonde", package = "MatchIt")
fit <- oal(treat ~ age + educ + married + re74, data = lalonde,
           outcome = ~ re78)
w <- oal_weight(fit)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=oal]{oal()}}, \code{\link[=oal_match]{oal_match()}}, \code{\link[WeightIt:weightit]{WeightIt::weightit()}},
\code{\link[WeightIt:get_w_from_ps]{WeightIt::get_w_from_ps()}}
}
