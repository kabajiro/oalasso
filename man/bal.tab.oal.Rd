% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baltab.R
\name{bal.tab.oal}
\alias{bal.tab.oal}
\title{Balance tables for oal objects}
\usage{
\method{bal.tab}{oal}(x, ...)
}
\arguments{
\item{x}{An \code{oal} object.}

\item{...}{Further arguments passed on to \code{\link[cobalt:bal.tab]{cobalt::bal.tab()}} (e.g.,
\code{un = TRUE}, \code{thresholds = c(m = 0.1)}).}
}
\value{
A \code{bal.tab} object; see \code{\link[cobalt:bal.tab]{cobalt::bal.tab()}}.
}
\description{
A method for \code{\link[cobalt:bal.tab]{cobalt::bal.tab()}}: assesses balance on the (original-scale)
covariates of an \code{\link[=oal]{oal()}} fit under the inverse-probability weights implied
by the outcome-adaptive lasso propensity score, which is supplied as a
\code{distance} measure.
}
\details{
The call delegates to the default \pkg{cobalt} machinery as
\verb{cobalt::bal.tab(<covariates>, treat = x$treat, weights = x$weights, s.d.denom = <by estimand>, distance = data.frame(ps = x$ps), ...)}, so all
the usual \pkg{cobalt} arguments (\code{un}, \code{stats}, \code{thresholds}, ...) are
available and display conventions are \pkg{cobalt}'s own. The \emph{selection
criterion} inside \code{\link[=oal]{oal()}} is the papers' wAMD instead, computed natively on
the standardized covariates; see \code{\link[=oal_wamd]{oal_wamd()}}.
}
\examples{
\dontshow{if (requireNamespace("MatchIt", quietly = TRUE)) withAutoprint(\{ # examplesIf}
data("lalonde", package = "MatchIt")
fit <- oal(treat ~ age + educ + married + re74, data = lalonde,
           outcome = ~ re78)
cobalt::bal.tab(fit, un = TRUE)
\dontshow{\}) # examplesIf}
}
\references{
Shortreed SM, Ertefaie A (2017). Outcome-adaptive lasso: variable selection
for causal inference. \emph{Biometrics}, 73(4), 1111-1122.
\doi{10.1111/biom.12679}
}
\seealso{
\code{\link[=oal]{oal()}}, \code{\link[cobalt:bal.tab]{cobalt::bal.tab()}}, \code{\link[=plot.oal]{plot.oal()}}
}
