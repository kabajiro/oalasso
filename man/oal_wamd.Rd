% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wamd.R
\name{oal_wamd}
\alias{oal_wamd}
\title{Weighted absolute mean difference (wAMD) of Shortreed and Ertefaie (2017)}
\usage{
oal_wamd(
  ps,
  treat,
  covs,
  coef,
  estimand = c("ATE", "ATT"),
  clip = c(0.01, 0.99)
)
}
\arguments{
\item{ps}{Numeric vector of propensity scores, strictly inside (0, 1)
before clipping.}

\item{treat}{Treatment vector: numeric 0/1, logical, or a two-level
factor/character (second level = treated), as in \code{\link[=oal]{oal()}}.}

\item{covs}{Numeric matrix of covariates, one row per unit; no missing
values.}

\item{coef}{Numeric vector of outcome-model coefficients, one per column of
\code{covs} (matched by name when both are named, else by position). Absolute
values are used as the balance weights.}

\item{estimand}{\code{"ATE"} (default) or \code{"ATT"}; selects the IPW weight
formula above.}

\item{clip}{Length-2 numeric; the scores are clipped to
\verb{[clip[1], clip[2]]} \strong{before} the weights are formed (default
\code{c(0.01, 0.99)}).}
}
\value{
A list with elements
\describe{
\item{\code{total}}{the wAMD, \eqn{\sum_j |\beta_j| d_j};}
\item{\code{by.covariate}}{named numeric vector of the per-covariate
contributions \eqn{|\beta_j| d_j} (they sum to \code{total}).}
}
}
\description{
Scores an arbitrary candidate propensity score on the exact wAMD balance
criterion used by \code{\link[=oal]{oal()}} to select its tuning parameters. This function is
the package's \strong{single source of truth} for the criterion: the internal
grid search calls it verbatim, and exporting it lets any candidate score --
from any package -- be scored on the same yardstick (the mirror of
\code{psAve::psave_criteria()}).
}
\details{
The propensity scores are first clipped to \code{clip}; the inverse-probability
weights at \code{estimand} are then (with \eqn{e_i} the clipped score)
\deqn{ATE:\; w_i = A_i/e_i + (1 - A_i)/(1 - e_i), \qquad
      ATT:\; w_i = A_i + (1 - A_i)\, e_i/(1 - e_i).}
For each covariate column \eqn{j} the weighted absolute mean difference is
\deqn{d_j = \left| \frac{\sum_i w_i X_{ij} A_i}{\sum_i w_i A_i}
  - \frac{\sum_i w_i X_{ij} (1 - A_i)}{\sum_i w_i (1 - A_i)} \right|,}
and the criterion is \eqn{\mathrm{wAMD} = \sum_j |\beta_j|\, d_j}, summed
over \strong{all} columns of \code{covs} (including any the propensity score model
excluded), with \eqn{\beta_j} the outcome-model coefficients supplied in
\code{coef} (their absolute values are taken internally).

Inside \code{\link[=oal]{oal()}}, \code{covs} is the \strong{standardized} model matrix and \code{coef} the
outcome coefficients on that same standardized scale (the wAMD, like the
adaptive weights, is scale-dependent). The formula is implemented natively
-- never delegated to \pkg{cobalt} -- so that it reproduces the published
criterion exactly.
}
\examples{
set.seed(7)
X <- matrix(rnorm(200), 100, 2, dimnames = list(NULL, c("x1", "x2")))
A <- rbinom(100, 1, plogis(X[, 1]))
ps <- plogis(0.8 * X[, 1])
oal_wamd(ps, A, X, coef = c(x1 = 0.5, x2 = 0.1), estimand = "ATE")
}
\references{
Shortreed SM, Ertefaie A (2017). Outcome-adaptive lasso: variable selection
for causal inference. \emph{Biometrics}, 73(4), 1111-1122.
\doi{10.1111/biom.12679}
}
\seealso{
\code{\link[=oal]{oal()}}, \code{\link[=weights.oal]{weights.oal()}}
}
