% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{predict.oal}
\alias{predict.oal}
\title{Predict outcome-adaptive lasso propensity scores for new data}
\usage{
\method{predict}{oal}(object, newdata = NULL, type = c("ps", "link"), ...)
}
\arguments{
\item{object}{An \code{oal} object.}

\item{newdata}{A data frame containing the variables of the propensity
score formula, or \code{NULL} (default) for the in-sample values.}

\item{type}{\code{"ps"} (default): propensity scores clipped to the fit's
\code{clip}; \code{"link"}: the unclipped linear predictor.}

\item{...}{Ignored.}
}
\value{
A numeric vector with one value per row, named by rownames. For
\code{newdata = NULL} and \code{type = "ps"} this is exactly \code{object$ps}.
}
\description{
Computes propensity scores (or the linear predictor) for new observations
from the stored terms, factor levels, and ORIGINAL-scale coefficients of an
\code{\link[=oal]{oal()}} fit. Works without \code{keep.fits = TRUE} and for both \code{refit} modes;
the training standardization is never re-estimated (the original-scale
coefficients already absorb it). Missing values in \code{newdata} are an error.
}
\seealso{
\code{\link[=oal]{oal()}}, \code{\link[=coef.oal]{coef.oal()}}
}
