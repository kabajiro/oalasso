% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.oal}
\alias{plot.oal}
\title{Plot an oal object}
\usage{
\method{plot}{oal}(x, type = c("wamd", "coef", "balance"), ...)
}
\arguments{
\item{x}{An \code{oal} object.}

\item{type}{One of \code{"wamd"} (default), \code{"coef"}, \code{"balance"}.}

\item{...}{For \code{"balance"}, further arguments to \code{\link[cobalt:love.plot]{cobalt::love.plot()}}
(e.g., \code{thresholds = 0.1}); otherwise further graphical parameters passed
to the base plotting calls.}
}
\value{
For \code{"balance"}, the \code{ggplot} object from \code{\link[cobalt:love.plot]{cobalt::love.plot()}}
(invisibly, after printing); otherwise \code{x}, invisibly.
}
\description{
Three diagnostic displays for an \code{\link[=oal]{oal()}} fit:
\describe{
\item{\code{"wamd"}}{the wAMD selection criterion against the exponent
\eqn{\delta} (\code{lambda}), one curve per \code{lambda2} value for
\code{method = "goal"}; the selected point is marked. Requires
\code{keep.path = TRUE}.}
\item{\code{"coef"}}{the original-scale propensity score coefficient paths
against \eqn{\delta} at the selected \code{lambda2}, one line per covariate;
covariates driven to zero (instruments, noise) are visible as lines
absorbed into the axis. Requires \code{keep.path = TRUE}.}
\item{\code{"balance"}}{a Love plot of covariate balance before/after
weighting, via \code{\link[cobalt:love.plot]{cobalt::love.plot()}} (dispatched through
\code{\link[=bal.tab.oal]{bal.tab.oal()}}; \pkg{cobalt} is an Import, so always available).}
}
}
\seealso{
\code{\link[=oal]{oal()}}, \code{\link[=bal.tab.oal]{bal.tab.oal()}}, \code{\link[cobalt:love.plot]{cobalt::love.plot()}}
}
