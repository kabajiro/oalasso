% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oal.R
\name{oal}
\alias{oal}
\title{Outcome-adaptive lasso propensity scores}
\usage{
oal(
  formula,
  data,
  outcome,
  method = c("oal", "goal"),
  estimand = c("ATE", "ATT"),
  family = gaussian(),
  lambda = c(-10, -5, -2, -1, -0.75, -0.5, -0.25, 0.25, 0.49),
  gamma = NULL,
  gamma.factor = 2,
  lambda2 = c(0, 10^c(-2, -1.5, -1, -0.75, -0.5, -0.25, 0, 0.25, 0.5, 1)),
  outcome.coef = NULL,
  refit = FALSE,
  clip = c(0.01, 0.99),
  keep.path = TRUE,
  keep.fits = FALSE,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{formula}{A two-sided formula \code{treat ~ x1 + x2 + ...}, exactly as in
\code{\link[MatchIt:matchit]{MatchIt::matchit()}}. The right-hand side defines the propensity score
covariates; the same covariates (plus the treatment) form the outcome
model that supplies the adaptive penalty weights.}

\item{data}{A data frame containing the variables in \code{formula} and
\code{outcome}. Complete cases in all used variables are REQUIRED; any missing
value is an error naming the offending variables, never a silent row
drop.}

\item{outcome}{A one-sided formula \code{~ y} naming the outcome variable
(exactly one variable). Required unless \code{outcome.coef} is supplied, in
which case it is unused. A two-sided formula is an error: OAL derives its
penalty weights from an outcome model on the \emph{same} covariates as the
propensity score model, so there is no separate outcome design to
specify.}

\item{method}{\code{"oal"} (default): the outcome-adaptive lasso; \code{"goal"}: the
generalized outcome-adaptive lasso, which adds an elastic-net
\eqn{\lambda_2 \sum_j \alpha_j^2} term (grouping effect and numerical
stability under correlated covariates and near-positivity violations).}

\item{estimand}{\code{"ATE"} (default) or \code{"ATT"}; determines the
inverse-probability weights used inside the wAMD criterion and returned
in \code{weights}. The ATE default is Shortreed and Ertefaie's wAMD weighting
and deliberately differs from \code{psAve::psave()}'s ATT default.}

\item{family}{The outcome (weight) model family: \code{gaussian()} (default;
the specification validated by the papers' simulations) or \code{binomial()}.
\code{binomial()} emits a one-time warning: binomial outcome models are beyond
the simulations validated by Shortreed and Ertefaie (2017); GLM theory
per Balde (2025).}

\item{lambda}{Numeric vector of EXPONENTS \eqn{\delta}; the penalties
actually applied are \eqn{\lambda_n = n^{\delta}}. The default is
Shortreed and Ertefaie's grid
\code{c(-10, -5, -2, -1, -0.75, -0.5, -0.25, 0.25, 0.49)}. A guard warns when
\code{max(lambda) > 3}: such values look like raw penalty values, not
exponents (convert a raw grid via \code{delta = log(lambda_n)/log(n)}).}

\item{gamma}{\code{NULL} (default) pairs \eqn{\gamma} with each \eqn{\delta} as
\eqn{\gamma = 2(\code{gamma.factor} - \delta + 1)} (the
Shortreed-Ertefaie/rejoinder convention); a single number (e.g. \code{2.5})
fixes \eqn{\gamma} and crosses it with the whole \code{lambda} grid (the
Schnitzer-style fixed-\eqn{\gamma} mode). No vector form is accepted.}

\item{gamma.factor}{The convergence factor \code{gcf} in the pairing formula
(default \code{2}); ignored when \code{gamma} is given.}

\item{lambda2}{Numeric grid of elastic-net ridge constants for
\code{method = "goal"} only (supplying it otherwise is an error). Default
\code{c(0, 10^c(-2, -1.5, -1, -0.75, -0.5, -0.25, 0, 0.25, 0.5, 1))} -- the
author's published grid (Balde 2025 supplement, "taken from Zou and
Hastie (2005)"), verified against the official GOAL code on 2026-07-02;
\code{0} is always a member and nests plain OAL exactly. \code{lambda2} is
selected jointly with \verb{(lambda, gamma)} by the wAMD.}

\item{outcome.coef}{The extension hook: a NAMED numeric vector of outcome
coefficients \eqn{b_j} over the \strong{standardized} model-matrix columns
(names must exactly cover the columns), overriding the internal outcome
model. \strong{The values are interpreted on the standardized scale of the
internal design matrix} -- adaptive weights are scale-dependent. All
values must be finite and not all zero. Zeros ARE allowed and mean a
hard drop: \eqn{|0|^{-\gamma} = \infty} and glmnet natively converts an
infinite penalty factor to an exclusion. No epsilon or cap is ever
applied. Using this hook flips the provenance label to
"screening use only" (see Details) and sets
\code{info$gamma.mode = "user-coef"}. If \code{outcome} is also supplied, it is
still validated and the outcome-leakage guard still runs (the outcome
variable may not appear among the PS covariates); if \code{outcome} is
omitted, the package \strong{cannot} detect outcome leakage in \code{formula} --
ensuring the coefficients were derived without post-treatment or outcome
information is then entirely the caller's responsibility.}

\item{refit}{\code{FALSE} (default): the propensity score comes from the
penalized fit itself (the Shortreed-Ertefaie/rejoinder convention).
\code{TRUE}: at every grid point an \emph{unpenalized} logistic regression is refit
on the selected covariates, and both the propensity score and the wAMD
come from the refit (the Schnitzer et al. 2025 variant, adapted from a
longitudinal setting); per-grid-point convergence is recorded in
\code{path$refit.converged} and any non-convergence triggers one warning --
never a silent fallback.}

\item{clip}{Length-2 numeric: propensity scores are clipped to
\verb{[clip[1], clip[2]]} BEFORE the wAMD IPW weights are formed and in the
returned \code{ps} (default \code{c(0.01, 0.99)}, equal to \code{psAve::psave()}'s
default so that psave's re-clipping is a no-op). The default is a
Shortreed-Ertefaie-lineage safety choice; Balde's official reference
code runs UNCLIPPED weights (\code{1/e}, \code{1/(1 - e)} with no truncation).
To reproduce that behavior, effectively disable clipping with
\code{clip = c(1e-12, 1 - 1e-12)}.}

\item{keep.path}{If \code{TRUE} (default), the full per-grid-point results are
stored in \code{path}, \code{sets}, and \code{coef.path}.}

\item{keep.fits}{If \code{TRUE}, the fitted \pkg{glmnet} paths, the outcome
model, and (with \code{refit = TRUE}) the selected refit are retained in
\code{fits}. Default \code{FALSE}.}

\item{verbose}{If \code{TRUE}, progress messages report the grid evaluation and
the selected point.}

\item{...}{Reserved for future use; supplying unused arguments triggers a
warning.}
}
\value{
An object of class \code{"oal"}: a list with components
\describe{
\item{\code{ps}}{numeric(n), named by \code{rownames(data)}: the propensity score,
clipped to \code{clip}, strictly in (0, 1) -- \strong{the deliverable}.}
\item{\code{weights}}{numeric(n): the IPW weights at \code{estimand} implied by
\code{ps}.}
\item{\code{coefficients}}{named numeric(p+1): selected propensity score
coefficients on the ORIGINAL covariate scale (intercept first).}
\item{\code{coefficients.std}}{the same on the standardized scale actually
optimized.}
\item{\code{selected}}{data frame, one row per model-matrix column: \code{term},
\code{outcome.coef} (signed \eqn{b_j}, standardized scale),
\code{penalty.factor}, \code{coef} (original scale), \code{selected} (logical), and
\code{role} (\code{"retained"}/\code{"excluded"}).}
\item{\code{lambda}}{named numeric: \code{delta}, \code{lambda.n} (\eqn{= n^\delta};
\eqn{= (n + p)^\delta} on GOAL grid points with \eqn{\lambda_2 > 0}),
\code{gamma}, \code{lambda2} (\code{NA} for \code{method = "oal"}) at the selected point.}
\item{\code{criterion}, \code{criterion.value}}{\code{"wamd"} and its value at the
selection.}
\item{\code{path}}{data frame (or \code{NULL} without \code{keep.path}), one row per
grid point in grid order: \code{lambda2}, \code{delta}, \code{lambda.n}, \code{gamma}, \code{s}
(the exact glmnet \code{s} used), \code{wamd}, \code{n.selected}, \code{refit.converged}
(\code{NA} unless \code{refit}), \code{selected}.}
\item{\code{sets}}{list (or \code{NULL}): the selected variable set at every grid
point.}
\item{\code{coef.path}}{(p+1) x G matrix (or \code{NULL}): propensity score
coefficients across the grid, original scale.}
\item{\code{outcome.coef}}{named numeric(p): the \eqn{b_j} actually used
(internal or user), standardized scale.}
\item{\code{outcome.model}}{compact list: coefficient table, \code{family}, and a
\code{label} (\code{"lm(y ~ A + X)"}, \code{"glm(y ~ A + X, binomial)"}, or
\code{"user-supplied"}); the full fit object only under \code{keep.fits}.}
\item{\code{balance}}{data frame per ORIGINAL covariate column: \code{smd.un},
\code{smd.wt}, \code{ks.un}, \code{ks.wt} via \pkg{cobalt} -- identical layout to
\code{psAve::psave()$balance}.}
\item{\code{provenance}}{the fixed provenance label, printed first by
\code{print()}/\code{summary()}.}
\item{\code{treat}}{integer(n) 0/1 treatment as coerced.}
\item{\code{covs}}{n x p numeric original-scale model matrix with
\code{attr(, "bin.vars")}.}
\item{\code{estimand}, \code{method}, \code{refit}, \code{clip}}{as resolved.}
\item{\code{formula}, \code{data}, \code{terms}, \code{xlevels}}{stored to power
\code{\link[=oal_match]{oal_match()}}, \code{\link[=oal_weight]{oal_weight()}} and \code{\link[=predict.oal]{predict.oal()}}.}
\item{\code{fits}}{list (or \code{NULL}): \code{glmnet} (one path per
\eqn{(\lambda_2, \gamma)}), \code{outcome}, \code{refit} -- iff \code{keep.fits}.}
\item{\code{info}}{list: \code{n}, \code{n.treated}, \code{p}, \code{grid.size}, \code{family},
\code{gamma.mode} (\code{"paired"}/\code{"fixed"}/\code{"user-coef"}), \code{center}, \code{scale},
\code{contrasts}, \code{glmnet.version}, \code{oalasso.version}.}
\item{\code{call}}{the matched call.}
}
}
\description{
\code{oal()} estimates a propensity score by the outcome-adaptive lasso (OAL) of
Shortreed and Ertefaie (2017) or its generalized elastic-net form (GOAL) of
Balde, Yang and Lefebvre (2023): an adaptive lasso on the treatment
(propensity score) log-likelihood whose penalty weights come from an
\emph{outcome} regression, so that covariates unrelated to the outcome --
instruments and noise variables -- are excluded from the propensity score
model. Tuning parameters are selected by the papers' weighted absolute mean
difference (wAMD) balance criterion. The result is deliberately modest: a
numeric score vector designed to be handed to \code{\link[MatchIt:matchit]{MatchIt::matchit()}} as
\code{distance}, to \code{\link[WeightIt:weightit]{WeightIt::weightit()}} as \code{ps}, or to \code{psAve::psave()} as an
appended candidate.
}
\details{
\subsection{Objective and exact glmnet evaluation}{

OAL solves, on the TOTAL log-likelihood scale,
\deqn{\hat\alpha = \arg\min_\alpha\; -\ell(\alpha; A, X_s)
  + \lambda_n \sum_j |b_j|^{-\gamma} |\alpha_j|,}
where \eqn{\ell} is the binomial log-likelihood of treatment on the
standardized covariates and \eqn{b_j} are the outcome-model coefficients.
\pkg{glmnet} minimizes the \emph{per-observation} loss and internally rescales
penalty factors to sum to the number of variables, so each grid point is
evaluated \strong{exactly} at
\deqn{s = \overline{\mathrm{pen}} \cdot \lambda_n / n, \qquad
  \mathrm{pen}_j = |b_j|^{-\gamma},}
via \code{coef(fit, s = s, exact = TRUE, x = , y = , penalty.factor = )}, which
re-solves at that penalty (the KKT-verified recipe of the
Shortreed/Ertefaie rejoinder code). When \code{outcome.coef} contains zeros,
some \eqn{\mathrm{pen}_j = \infty}: glmnet converts those to exclusions and
internally resets their factor to 1 \emph{before} the sum-to-nvars rescale, so
the constant generalizes to
\eqn{s = (\sum_{j: \mathrm{finite}} \mathrm{pen}_j + \#\{\mathrm{pen}_j =
\infty\})/p \cdot \lambda_n / n} -- identical to
\eqn{\overline{\mathrm{pen}}\,\lambda_n/n} when all factors are finite.
This correction is always on; it is what makes the \eqn{n^\delta} grid
carry its published meaning.
}

\subsection{Standardization protocol (fixed, not an argument)}{

The model matrix \code{X <- model.matrix(formula, data)[, -1]} (default
treatment contrasts) is standardized ONCE, \code{Xs <- scale(X, TRUE, TRUE)},
all columns including dummies (the rejoinder convention). The outcome model
AND glmnet are both fit on this SAME \code{Xs} with
\code{glmnet(standardize = FALSE)}. Per-subset scaling is structurally
impossible. Coefficients are back-transformed for output:
\eqn{\alpha_j = \alpha^{std}_j / s_j} and
\eqn{\alpha_0 = \alpha^{std}_0 - \sum_j \alpha^{std}_j c_j / s_j}, with
centers \eqn{c} and scales \eqn{s} stored in \code{info}.
}

\subsection{Weight (outcome) model and the degenerate-beta policy}{

By default \eqn{b} comes from \code{lm(y ~ A + Xs)} (or
\code{glm(y ~ A + Xs, binomial)}) on the FULL sample, both arms pooled -- the
Shortreed-Ertefaie/rejoinder convention (not control-arm-only). Only the
covariate coefficients are used; the treatment coefficient is discarded and
the treatment is never penalized anywhere. Any \code{NA} or \emph{exactly zero}
internal coefficient is an ERROR naming the columns: OLS betas are almost
surely nonzero, so an exact zero or \code{NA} signals rank deficiency or
aliasing that would silently corrupt the penalty-scale correction. Remove
the collinear columns or supply \code{outcome.coef}.
}

\subsection{Tuning grid and the \eqn{\gamma} pairing}{

\code{lambda} supplies exponents \eqn{\delta} with
\eqn{\lambda_n = n^{\delta}}, \code{n = nrow(model.matrix)} always. With
\code{gamma = NULL} each \eqn{\delta} is paired with
\eqn{\gamma = 2(\mathrm{gcf} - \delta + 1)} (default
\eqn{\mathrm{gcf} = 2}), the formula derivable from
\eqn{\lambda_n\, n^{\gamma/2 - 1} = n^{\mathrm{gcf}}}; there are no
\eqn{\lambda \times \gamma} cross-products in paired mode. A scalar
\code{gamma} is crossed with all \eqn{\delta} (Schnitzer's \eqn{\gamma = 2.5}
is reachable this way; convert their raw \eqn{\lambda} grid via
\eqn{\delta = \log \lambda_n / \log n}).
}

\subsection{GOAL}{

For \eqn{\lambda_2 > 0} the elastic-net term is solved by the Zou-Hastie
augmentation: \eqn{X_{aug} = \mathrm{rbind}(X_s, \sqrt{\lambda_2} I_p)}
with \eqn{p} pseudo-responses 0, followed by rescaling ALL coefficients
INCLUDING the intercept by \eqn{(1 + \lambda_2)} (Balde 2025 supplement:
\eqn{PS = \mathrm{expit}(\mathrm{cbind}(1, X)\,(1 + \lambda_2)\,\hat\alpha)});
the propensity score uses the original \eqn{n} rows only. On augmented
grid points the raw penalty constant is
\eqn{\lambda_n = (n + q)^{\delta}} with \eqn{q = p} augmentation rows
(Balde's \code{adaptive.lasso(lambda = n.q^(il))}, \code{n.q = n + q}), and the
penalty-scale constant uses \eqn{1/(n + q)} in place of \eqn{1/n}, i.e.
\eqn{s = \overline{\mathrm{pen}} (n + q)^{\delta} / (n + q)}.
\eqn{\lambda_2 = 0} grid points run the plain-OAL code path (no
augmentation, \eqn{\lambda_n = n^{\delta}}), so \code{method = "goal"} with
\code{lambda2 = 0} nests \code{method = "oal"} exactly. (Balde's script literally
augments with \eqn{q} zero rows and the \eqn{(n+q)^\delta} base even at
\eqn{\lambda_2 = 0}; the zero rows still shift the intercept, so exact
nesting is deliberately preferred here.) \eqn{\lambda_2} is selected
jointly with \eqn{(\delta, \gamma)} by the wAMD: the flat first-minimum
over the \eqn{\lambda_2}-outer \eqn{\times} \eqn{\delta}-inner grid,
which is equivalent to Balde's nested rule (per-\eqn{\lambda_2} minima
over \eqn{(\delta, \gamma)}, then the first minimum over
\eqn{\lambda_2}).
}

\subsection{wAMD criterion and selection}{

Every grid point is scored by \code{\link[=oal_wamd]{oal_wamd()}}: propensity scores clipped to
\code{clip} first (note that Balde's reference implementation is unclipped;
see the \code{clip} argument), IPW weights at \code{estimand}, per-column weighted
absolute mean differences on the standardized matrix summed with weights
\eqn{|b_j|} over ALL columns (including excluded ones). The selected grid point is the
FIRST minimum (relative tolerance \code{1e-9}) in the grid order
\eqn{\lambda_2} ascending (0 first) outer \eqn{\times} \eqn{\delta}
ascending inner -- ties therefore prefer plain OAL and then the smallest
penalty. The wAMD is a balance heuristic: no published proof guarantees it
lands \eqn{\lambda_n} inside the window required by the theory
(\eqn{\lambda_n/\sqrt{n} \to 0},
\eqn{\lambda_n n^{\gamma/2 - 1} \to \infty}, \eqn{\gamma > 1}), and it
inherits the outcome model's misspecification vulnerability.
}

\subsection{Why exclude instruments?}{

Covariates that predict treatment but not outcome should be excluded from
a propensity score model: they increase the variance of the effect estimate
without decreasing bias (Brookhart et al. 2006) and amplify the bias from
any unmeasured confounding (Myers et al. 2011). OAL's penalty implements
exactly this exclusion.
}

\subsection{Provenance labels}{

\code{print()} and \code{summary()} always print a provenance line first:
\code{"OAL (Shortreed & Ertefaie 2017, doi:10.1111/biom.12679)"} or
\code{"GOAL (Balde, Yang & Lefebvre 2023, doi:10.1111/biom.13683)"} with a
clause stating whether the \code{lambda2} grid is the author's published grid
(Balde 2025 supplement) or user-specified. Supplying \code{outcome.coef} appends
\code{"user-supplied outcome coefficients — screening use only; no oracle property"}; \code{family = binomial()} and \code{refit = TRUE} append their own
flags.
}

\subsection{Handoff contract}{

\verb{$ps} is a numeric vector of length \code{n}, named by \code{rownames(data)},
strictly inside (0, 1) after clipping -- it satisfies
\code{psAve::psave(ps.append = )}, \code{MatchIt::matchit(distance = )}, and
\code{WeightIt::weightit(ps = )} by construction.

There is no \code{seed} argument: the whole pipeline is deterministic.
}
}
\examples{
\dontshow{if (requireNamespace("MatchIt", quietly = TRUE)) withAutoprint(\{ # examplesIf}
data("lalonde", package = "MatchIt")

## 1) Matching
fit <- oal(treat ~ age + educ + race + married + nodegree + re74 + re75,
           data = lalonde, outcome = ~ re78)
fit
m <- oal_match(fit)   # = MatchIt::matchit(f, lalonde, distance = fit$ps)

## 2) Weighting (requires WeightIt)
# w <- oal_weight(fit)  # = WeightIt::weightit(f, lalonde, ps = fit$ps, "ATE")

## 3) psAve composition (requires psAve); cbind() labels the candidate
##    while keeping the row names aligned with rownames(data)
# ma <- psAve::psave(treat ~ age + educ + race + married + nodegree +
#                      re74 + re75,
#                    data = lalonde, outcome = ~ re78,
#                    ps.append = cbind(oal = fit$ps))
\dontshow{\}) # examplesIf}
}
\references{
Shortreed SM, Ertefaie A (2017). Outcome-adaptive lasso: variable selection
for causal inference. \emph{Biometrics}, 73(4), 1111-1122.
\doi{10.1111/biom.12679}

Balde I, Yang Y, Lefebvre G (2023). Reader reaction to "Outcome-adaptive
lasso: variable selection for causal inference" by Shortreed and Ertefaie
(2017). \emph{Biometrics}, 79(1), 514-520. \doi{10.1111/biom.13683}

Jones B, Ertefaie A, Shortreed SM (2023). Rejoinder to reader reaction "On
the use of the outcome-adaptive lasso for propensity score estimation".
\emph{Biometrics}, 79(1), 521-525. \doi{10.1111/biom.13681}

Balde I (2025). Oracle properties of the generalized outcome-adaptive
lasso. \emph{Statistics & Probability Letters}.
\doi{10.1016/j.spl.2025.110379}

Zou H (2006). The adaptive lasso and its oracle properties. \emph{Journal of the
American Statistical Association}, 101(476), 1418-1429.
\doi{10.1198/016214506000000735}

Brookhart MA, Schneeweiss S, Rothman KJ, Glynn RJ, Avorn J, Sturmer T
(2006). Variable selection for propensity score models. \emph{American Journal
of Epidemiology}, 163(12), 1149-1156. \doi{10.1093/aje/kwj149}

Myers JA, Rassen JA, Gagne JJ, Huybrechts KF, Schneeweiss S, Rothman KJ,
Joffe MM, Glynn RJ (2011). Effects of adjusting for instrumental variables
on bias and precision of effect estimates. \emph{American Journal of
Epidemiology}, 174(11), 1213-1222. \doi{10.1093/aje/kwr364}

Schnitzer ME, Talbot D, Liu Y, Berger C, Wang G, O'Loughlin J, Sylvestre
M-P, Ertefaie A (2025). Outcome-adaptive LASSO for longitudinal data.
\emph{Statistics in Medicine} (arXiv:2410.08283).
}
\seealso{
\code{\link[=oal_match]{oal_match()}}, \code{\link[=oal_weight]{oal_weight()}}, \code{\link[=oal_wamd]{oal_wamd()}}, \code{\link[=predict.oal]{predict.oal()}},
\code{\link[=plot.oal]{plot.oal()}}, \code{\link[=bal.tab.oal]{bal.tab.oal()}}
}
